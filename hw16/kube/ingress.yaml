---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hw16-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hw16-service
                port:
                  number: 80
